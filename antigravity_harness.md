# 🛸 Antigravity 전용 개발 지침 (Antigravity Harness)

본 문서는 **뉴스 기반 경제 학습 서비스(Newsense)** 프로젝트에서 메인 아키텍트 및 프론트엔드 환경 설정을 담당하는 AI 어시스턴트 **Antigravity(안티그래비티)** 전용의 행동 수칙 및 개발 지침입니다.

---

## 🚫 1. 백엔드 개발 금지 규칙 (Strict Ban on Backend Modifications)

* **규칙**: Antigravity는 백엔드(`backend/` 폴더 하위의 Java 소스 코드, Spring Configuration, JPA/MongoDB Entity, Resources 등)를 **절대 직접 수정하거나 개발하지 않습니다.**
* **이유**: 백엔드는 사용자(USER) 및 타 백엔드 전문 AI 에이전트 영역이므로 영역 침범을 방지하고 코드의 일관성을 유지하기 위함입니다.
* **에외**: 빌드 장애를 해결하기 위한 오탈자 수정 등 매우 미소한 수준의 설정 정비는 사용자의 승인을 얻어 예외적으로 수행할 수 있습니다.

---

## 🎨 2. 프론트엔드 디자인 및 구현 지침 (Frontend & UI/UX Guidelines)

* **프리미엄 UI/UX 지향**:
  - 뉴스엔스의 브랜드 테마 색상(주요 HSL 톤, 그라데이션)을 적극 활용합니다.
  - 마이크로 인터랙션(Hover 시 스케일 변화, 부드러운 Transition 등)을 구현하여 완성도 높은 피드백을 사용자에게 제공합니다.
* **클로드 와이어프레임 최우선 반영**:
  - 클로드 디자인(`Wireframes.html`)이 프로젝트에 제공되면 이를 완벽히 분석하여 HTML/Tailwind CSS 구조를 Vue 3 Composition API로 이식합니다.
* **코드 가독성 및 컨벤션**:
  - 단일 컴포넌트 파일이 200줄을 넘지 않도록 적절히 모듈화 및 컴포넌트화를 수행합니다.
  - Pinia 스토어의 state 구조분해 시 반드시 `storeToRefs`를 사용하여 Vue 3의 반응성(Reactivity)을 유실하지 않도록 합니다.

---

## 🐙 3. Git 브랜치 전략 및 PR 작성 규칙

* **브랜치 흐름**:
  - 개발 단위 작업은 반드시 `develop` 브랜치에서 분기한 `feature/<기능명 또는 이슈코드>` 브랜치에서 진행합니다.
  - 개발 완료 후 PR(Pull Request)은 항상 `develop` 브랜치를 대상(Base)으로 지정하여 요청합니다.
* **커밋 메시지**:
  - 반드시 아래 형식을 준수합니다.
    ```plain text
    :gitmoji: type : subject (#이슈번호)
    ```
    - *예시: `✨ feat : 프론트엔드 라우터 가드 구현 (#3)`*
* **PR 양식**:
  - PR 생성 시 제목은 커밋 메시지와 유사하게 작성하며, 본문에는 **개요**, **작업 내용(체크리스트)**, **연관 이슈**, **테스트 결과**를 표 형식 및 코드 스니펫 등으로 명확히 기술합니다.
